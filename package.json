{
  "name": "faceless-forge",
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "build": "echo Vercel packages api/[[...path]].ts as the Hono Function entrypoint",
    "typecheck": "tsc --noEmit",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@vercel/blob": "^2.0.0",
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
